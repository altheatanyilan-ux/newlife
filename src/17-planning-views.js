/* placeholder — replaced in the next commit */
function planCalendarHTML(){ return '<div class="empty">Calendar view, next.</div>'; }
function planKanbanHTML(){ return '<div class="empty">Board view, next.</div>'; }
function planMatrixHTML(){ return '<div class="empty">Matrix view, next.</div>'; }
function planTimelineHTML(){ return '<div class="empty">Timeline view, next.</div>'; }
function bindPlanViews(){}
function planHabitsHTML(){ return '<div class="empty">Habits, next.</div>'; }
function bindPlanHabits(){}
function planStatsHTML(){ return '<div class="empty">Statistics, next.</div>'; }
function openFocusTimer(){ toast('The focus timer arrives in the next pass.'); }
