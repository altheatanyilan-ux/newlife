/* placeholder — the focus timer, habits and statistics land in the next commit */
function planHabitsHTML(){ return '<div class="empty">Habits, next.</div>'; }
function bindPlanHabits(){}
function planStatsHTML(){ return '<div class="empty">Statistics, next.</div>'; }
function openFocusTimer(){ toast('The focus timer arrives in the next pass.'); }
