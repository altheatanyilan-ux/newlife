node_modules/

# scratch drivers: renderers, screenshotters, the frame-time probe, the
# runner that walks the whole smoke suite
_*.js
_*.sh

# the recording-sync harness decodes the orchestra samples here once
tools/.cache/
